name: Diagnóstico mínimo do runner

on:
  push:
    branches:
      - audit/higienizacao-profunda-20260726
  workflow_dispatch:

jobs:
  probe:
    runs-on: ubuntu-22.04
    timeout-minutes: 2
    steps:
      - name: Confirmar alocação do runner
        run: |
          echo "RUNNER_OK"
          uname -a
